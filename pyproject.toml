[tool.poetry]
name = "coupadownloads"
version = "0.1.0"
description = ""
authors = ["CzarX86 <julio.cezar@outlook.com>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12"
selenium = ">=4.0.0"
requests = ">=2.31.0"
pytest = ">=7.0.0"
pytest-mock = ">=3.10.0"
pandas = ">=1.5.0"
openpyxl = ">=3.0.0"
psutil = ">=5.8.0"
pytest-cov = ">=4.0.0"
pytest-xdist = ">=3.0.0"
pytest-timeout = ">=2.0.0"
pytest-html = ">=3.0.0"
freezegun = ">=1.2.0"
factory-boy = ">=3.2.0"
faker = ">=18.0.0"
extract-msg = ">=0.41.0"
weasyprint = ">=60.0"
pdfkit = ">=1.0.0"
webdriver-manager = "^4.0.2"

[tool.poetry.scripts]
coupa-downloader = "src.main:main"



[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
